from random import shuffle
from deck import d 
class Pioche():
    def __init__(self, liste):
        self.liste = liste

    def pioche(nb):
        lo = list()
        for k in range(nb):
            